// //~ Import style
// import './Projects.scss';

// import { motion, AnimatePresence } from 'framer-motion';

// const Projects = () => {
//   const transformOptions = JSON.stringify({
//     r: 0,
//     s: 0,
//     tx: 0,
//     ty: 0,
//   });

//   return (
//     <section className="projects">
//       <h1>Projets</h1>

//       <motion.div className="container">
//         <motion.div className="buttons color-04" data-scroll-transform={transformOptions} exit={{ translateX: -200, opacity: 0, transition: { duration: 0.2 } }}></motion.div>
//         <motion.div className="buttons color-04" data-scroll-transform={transformOptions} exit={{ translateX: -200, opacity: 0, transition: { duration: 0.4 } }}></motion.div>
//         <motion.div className="buttons color-04" data-scroll-transform={transformOptions} exit={{ translateX: -200, opacity: 0, transition: { duration: 0.6 } }}></motion.div>
//         <motion.div className="buttons color-04" data-scroll-transform={transformOptions} exit={{ translateX: -200, opacity: 0, transition: { duration: 0.8 } }}></motion.div>
//         <motion.div className="buttons color-04" data-scroll-transform={transformOptions} exit={{ translateX: -200, opacity: 0, transition: { duration: 1 } }}></motion.div>
//         <motion.div className="buttons color-04" data-scroll-transform={transformOptions} exit={{ translateX: -200, opacity: 0, transition: { duration: 1 } }}></motion.div>
//         <motion.div className="buttons color-04" data-scroll-transform={transformOptions} exit={{ translateX: -200, opacity: 0, transition: { duration: 1 } }}></motion.div>
//         <motion.div className="buttons color-04" data-scroll-transform={transformOptions} exit={{ translateX: -200, opacity: 0, transition: { duration: 1 } }}></motion.div>
//         <motion.div className="buttons color-04" data-scroll-transform={transformOptions} exit={{ translateX: -200, opacity: 0, transition: { duration: 1 } }}></motion.div>
//         <motion.div className="buttons color-04" data-scroll-transform={transformOptions} exit={{ translateX: -200, opacity: 0, transition: { duration: 1 } }}></motion.div>
//         <motion.div className="buttons color-04" data-scroll-transform={transformOptions} exit={{ translateX: -200, opacity: 0, transition: { duration: 1 } }}></motion.div>
//         <motion.div className="buttons color-04" data-scroll-transform={transformOptions} exit={{ translateX: -200, opacity: 0, transition: { duration: 1 } }}></motion.div>
//         <motion.div className="buttons color-04" data-scroll-transform={transformOptions} exit={{ translateX: -200, opacity: 0, transition: { duration: 1 } }}></motion.div>
//         <motion.div className="buttons color-04" data-scroll-transform={transformOptions} exit={{ translateX: -200, opacity: 0, transition: { duration: 1 } }}></motion.div>
//         <motion.div className="buttons color-04" data-scroll-transform={transformOptions} exit={{ translateX: -200, opacity: 0, transition: { duration: 1 } }}></motion.div>
//         <motion.div className="buttons color-04" data-scroll-transform={transformOptions} exit={{ translateX: -200, opacity: 0, transition: { duration: 1 } }}></motion.div>
//         <motion.div className="buttons color-04" data-scroll-transform={transformOptions} exit={{ translateX: -200, opacity: 0, transition: { duration: 1 } }}></motion.div>
//         <motion.div className="buttons color-04" data-scroll-transform={transformOptions} exit={{ translateX: -200, opacity: 0, transition: { duration: 1 } }}></motion.div>
//       </motion.div>
//     </section>
//   );
// };

// export default Projects;
